from aiogram.types.bot_command import BotCommand

from src.core.bot.bot import bot


async def set_commands(): ...
    # TODO:
    # commands = [
    #     BotCommand(command='TEXT', description='TEXT'),
    # ]
    # await bot.set_my_commands(commands)
